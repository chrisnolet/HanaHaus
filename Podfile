source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '7.0'
inhibit_all_warnings!

target 'Example' do
  # pod 'Analytics', '~> 1.8'        # Includes all subspecs (will be okay when it includes Optimizely 1.0)
  pod 'Analytics/Mixpanel', '~> 1.8' # Alternatively, include Mixpanel & Optimizely individually
  pod 'HexColors', '~> 2.2'
  pod 'Parse', '~> 1.5'
  pod 'SVProgressHUD', '~> 1.0'
end
